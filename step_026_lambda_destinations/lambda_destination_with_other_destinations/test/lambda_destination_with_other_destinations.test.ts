// import * as cdk from 'aws-cdk-lib';
// import { Template } from 'aws-cdk-lib/assertions';
// import * as LambdaDestinationWithOtherDestinations from '../lib/lambda_destination_with_other_destinations-stack';

// example test. To run these tests, uncomment this file along with the
// example resource in lib/lambda_destination_with_other_destinations-stack.ts
test('SQS Queue Created', () => {
//   const app = new cdk.App();
//     // WHEN
//   const stack = new LambdaDestinationWithOtherDestinations.LambdaDestinationWithOtherDestinationsStack(app, 'MyTestStack');
//     // THEN
//   const template = Template.fromStack(stack);

//   template.hasResourceProperties('AWS::SQS::Queue', {
//     VisibilityTimeout: 300
//   });
});
